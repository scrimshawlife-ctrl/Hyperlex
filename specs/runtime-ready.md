# Runtime-ready gate — mutation grammar v0.1

**Date:** 2026-09-04  
**Lane:** SHADOW / advisory  
**Does not promote constitution or merge to main.**

## Meaning of runtime-ready

Hermes can, after skill install + reload:

1. Route slang measurement to Hyperlex (not ECO generate).
2. Run `pipeline` offline and read `analysis.mutation_trace` when operators fire.
3. Run isolated `mutation trace` / `mutation predict` as JSON.
4. Keep `brier: null` and `forecast_eligible: false` on grammar packets.
5. Refuse wrap / jailbreak composition.

It does **not** mean GAME_ENCODE parsers, watch jsonl, `--human` cards, or main merge.

## Remaining before that bar

### Specs still to lock (small)

| Artifact | Why |
|----------|-----|
| `000` converge note | Spine is as-built; mark CONVERGE or leave SPECIFY with a one-liner that implement of 001 does not mutate API_V1 |
| `002` tasks + checklist | Command surface specify exists; no tasks file |
| `result.v1` additive mention | `mutation_trace` is additionalProperties today; document allowed attach |
| `hyperlex.manifest.yaml` | intents/commands/schemas omit mutation |
| skill CLI alias | `scripts/hyperlex.py` still only `mutation-predict` |

### Specs deferred (v0.2, not this gate)

- GAME_ENCODE / CODE_SWITCH / PHONETIC_WARP parsers
- `mutation watch` + `~/.hyperlex/mutation_watch.jsonl`
- `--human` card helper
- `$HLX commands` router fields (`layer`, `noun`, `forecast_eligible`, …)
- Live red-team corpora
- Constitution promotion off SHADOW

## Apply / execute order

1. Manifest + schema list (hosts that read yaml).
2. Thin skill-tree alias if fat CLI cannot move this cycle (`scripts/hlx-mutation` → package `cmd_mutation`).
3. Document result.v1 attach.
4. Operator review of T11 + constitution VII–X.
5. Merge decision (human).
6. Only then v0.2 specify.

## Done on this branch (do not redo)

- constitution I–X
- 001 spec, clarify C1–C10, threat, OWASP, dual-use gate
- 002 specify
- `hyperlex.mutation` parser + tests
- fail-open attach via signal_report
- package CLI noun
- SKILL.md routing + slang-emulation handoff (local skill tree)
- `parse_mutation_trace` on package root
