# Spec 007 — next: HOLD (morph73 REJECT; residual AUTHORIZE=0)

`name_gate=false`. BEST=**morph65** (held). Upsample freeze = **11+**. Do not run SECOND_SLOT=4.

## Done

1. morph69–73 REJECT. Residual AUTHORIZE=0 on each.
2. morph73: `INIT_EXPAND_VOCAB=1` warm morph65; best **0.9649122807017544** == fair n=171 → REJECT (tie). E2 PASS. Expand applied (roles +2 / fillers +26).

## Next

**Hold.** No morph74 without a new legal one-knob. Do not burn force_added=0 / fair-tie / identical expand-warm replay. Residual METHOD morph43 AUTHORIZE=0 (scaffolding).

Qwen stays stopped unless the operator re-enables `qwen38-27b.service`.
