# Hyperlexical participation in persistent-agent research

Program: `ABX-NOEMA-REP-001`  
Status: proposed / SHADOW / ADVISORY_ONLY  
Date: 2026-09-14  
Scope: specification review only; no runtime or permission change.

[Shared program draft](https://github.com/scrimshawlife-ctrl/Abraxas/blob/codex/persistent-agent-program-20260914/docs/research/persistent-agent-program/spec.md) owns cross-repository experiment coordination, workflows WF-P01 through WF-P05, acceptance AC-P01 through AC-P10, and the candidate sidecar. This document owns only this repository's participation mapping. The program is not a new specialist or a replacement for existing contracts. Draft branch links are review links; pin the accepted commit when adopted.

## Existing authority and naming

[Spec 007](spec.md), [milestones](milestones.md), [engineering](engineering.md), and [constitution](../../.specify/memory/constitution.md) retain authority. Hyperlex is the transitional repository; Hyperlexical owns the encoder/train/evaluation product; ne0l0gist owns ingest/harvest. This candidate does not rename paths or environment variables.

## Contribution

Hyperlexical measures form and lexical structure on permitted experiment observations. The unchanged packet is hyperlex.hyperlexical.inference.v0.1: brier=null, forecast_eligible=false, routes_claimed limited to form/lexical. No semantic route, latent-truth assertion, sign-relation ownership or specialist registry write follows.

Noesis already has a transform adapter contract. Spec 007's encoder must not be assumed to generate approved paraphrases, translations or symbol transforms. A separate supported provider or operator-reviewed frozen fixture source must satisfy that contract. Declared semantic preservation is an experimental hypothesis, not a label minted from encoder confidence.

## Workflows and acceptance

J-P03 -> WF-P03 -> CAPTURED/ANALYZED or BLOCKED -> existing inference packet -> AC-P05/AC-P06 -> T-HLX-P01: map source, model and output hashes in the shared external sidecar. Preserve original packet bytes. Add no API_V1/API_EXTENDED behavior in this cycle.

J-P04 -> WF-P04 -> REVIEWED_RESULT/INCONCLUSIVE -> frozen evaluation manifest -> AC-P08 -> T-HLX-P02: identify approved data, family/structure partitions, labels, baseline code and candidate weights before an independent evaluation. Keep classification and unbinding claims separate. No E2, name_gate or T13 promotion follows from benchmark participation.

J-P05 -> WF-P05 -> DOSSIER_DRAFT -> resource receipts -> AC-P09 -> T-HLX-P03: once existing training permission is satisfied, record training duration, batch/context/input shape, framework/CUDA, peak memory, throughput and power when measured. Compare identical training and inference workloads across solo/combined operation; candidate scheduling gains remain projections.

## Data and experiment design

Use an untouched, hash-pinned test set with source/family/structure grouping and explicit leakage checks. Record OBSERVED and INFERRED labels separately, per-head supervision, weak-label handling and exclusion rules. Use at least three registered seeds for the proposed capacity/evaluation study, reporting every run and its failures; three seeds do not by themselves establish statistical adequacy. Size/uncertainty design depends on the registered endpoint and pilot variance. Never tune on the frozen confirmation set or repeatedly select checkpoints using its result.

The current Operator Hub is the location for changing training state: https://app.notion.com/p/3d73e8ba2f5c81ad89d7c2df8e931a83 . Do not copy changing morph scores into this program note. Existing weights/data remain local and governed; reviewed cards may reference private review bundles only with disclosure authority.

## Failure, governance and verification

Missing optional weights preserve the existing fail-open analyze path: omit the optional block and record dependent program measurement as NOT_COMPUTABLE. Missing required trained evidence blocks its claim; it must not be replaced by a stub result. Mock artifacts are conformance only. No new training, Hub upload, gold settlement, cron, 006 IsA, 008 implementation or route promotion is opened.

Verification: existing offline SHADOW boundary tests plus JSON/link/diff checks; no weight download during tests. GPU performance, final model eligibility and Noema export compatibility remain NOT_COMPUTABLE until measured.
