# Operator name — ne0l0gist (ingest) · 2026-09-24

`name_gate=false` (Hyperlexical). Soft_ceiling **ARMED**. BEST=**morph65**.

## Authority

Operator: **`name neologist as in repo`**.

## Decision

Public **ingest / harvest** product name is **`ne0l0gist`** — repo spelling with zeros (`ne` + `0` + `l` + `0` + `gist`), matching STATUS / README / SKILL / Spec 007 voice already on tip.

This is **not**:

- a Hyperlexical `name_gate` flip
- a Hub upload authorize
- permission to invent OBSERVED or re-settle morph77’s four

## Split (unchanged)

| Name | Role |
|------|------|
| **Hyperlex** | Transitional monorepo shell |
| **ne0l0gist** | Slang ingest / harvest / `ingest_tap` / export-settle (**named**) |
| **Hyperlexical** | Spec 007 model / train / eval / E2 — still behind `name_gate` |

## Next

Hyperlexical `name_gate` still needs an explicit Danny sentence (`name_gate` yes / “name Hyperlexical”). Climb still needs named phrases on a live empty card or a new `force_expand_safe` acquire.
