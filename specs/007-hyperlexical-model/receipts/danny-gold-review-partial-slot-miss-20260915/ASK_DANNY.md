# Ask for Danny — OBSERVED structure gold / ladder path

**Status:** waiting on Danny. **OBSERVED hold.** `name_gate=false`.  
**BEST:** morph19 **untouched**. Climb KEEP famcls52 ≠ BEST. **No train** until authorize.

## Clear ask

Please choose **one**:

1. **Authorize OBSERVED structure gold** for the residual spans in `candidates.jsonl`  
   (or a marked subset), under schemes **`positional` | `type_slot` only** — fill blank
   `gold_fillers` / `gold_roles`, set `authorized_observed_structure_gold=true` and
   `confirm_labels` / `confirm_rights` only when rights are real.

2. **Authorize an alternate ladder path** (explicit written path — not another silent
   famcls53 / morph train). Climb holdout is already saturated (famcls52 all 1.0).

Until (1) or (2): **no train**, no dump→accept→train, no famcls53, no morph envelope
rerun, no BEST overwrite.

## Why this package

| fact | value |
|------|------:|
| morph19 civilian `unbind_exact` | 0.4545 (cleared 0.45; **not** 0.55) |
| morph19 residual n | 198 / 363 val |
| dominant theme `partial_slot_miss` | **146** |
| climb holdout famcls52 | 1.0 / 1.0 / 1.0 / 1.0 — **no residuals** |
| famcls52→civilian transfer exact | 0.0028 |

## Non-goals

- Do **not** invent gold in this package (fields left blank on purpose).
- Do **not** treat `eval_reference_fillers` as authorized OBSERVED gold.
- Do **not** auto-promote INFERRED → OBSERVED.
- Do **not** start famcls53 / morph trains or touch BEST morph19.
