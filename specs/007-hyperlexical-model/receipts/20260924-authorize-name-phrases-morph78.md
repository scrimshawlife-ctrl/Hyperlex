# authorize name phrases — morph78 (2026-09-24)

**Authority:** operator `push-org then name phrases` (after org #14 merge).

## Interpretation

`name phrases` = explicit override naming phrases on the live morph78 HOLD card
(`receipts/morph78-fresh-acquire-label-hold-20260923/morph78_HOLD_AUTHORIZE_CARD.json`).

Sentence shape recorded:

```
authorize morph78: have fun staying poor, fr fr no cap
```

## Phrases (from card pool — not invented)

| # | norm | lineage | prior Jev | card conf |
|--:|------|---------|-----------|----------:|
| 1 | `have fun staying poor` | crypto-degen | force_expand_safe | 0.25 |
| 2 | `fr fr no cap` | kinship-address | force_expand_safe | 0.22 |

Source field: `jev_revisit77_safe_new` (below `card_high_conf_new` threshold 0.40 — empty card previously; operator override fills authorize list).

## Explicit exclusions

Do **not** re-settle morph77’s already OBSERVED four: `we're so back`, `clutch up`, `bet that up`, `real talk`.

Do **not** invent OBSERVED from the 35× `defer_quality` pool without a separate named list.

## Next (Spark)

1. `PACKET_SETTLE` / val-settle these 2 as OBSERVED `split=val` on operator SoT.
2. Force/hard expand from morph77 tip (232/273).
3. soft_ceiling decide (broad OBSERVED > PRIOR live + E2) — fair may stay 1.0 n=164.

`name_gate` remains **false**. This authorize does **not** flip Hyperlexical naming.
