<!-- orchestra-diagram.v1 framework=numogram -->
<!-- overlay=chaos-magic -->

```mermaid
flowchart LR
  potential["potential<br/>zone_0"]
  init["init<br/>zone_1"]
  structure["structure<br/>zone_4"]
  threshold["threshold<br/>zone_5"]
  feedback["feedback<br/>zone_6"]
  completion["completion<br/>zone_9"]
  potential --> init
  init --> structure
  structure --> threshold
  threshold --> feedback
  feedback --> completion
```

<!-- flows
  primary: numogram primary path :: potential → init → structure → threshold → feedback → completion
  core: numogram core collapse :: init → threshold → completion
  overlay-chaos-magic: overlay · chaos-magic :: potential → init → structure → threshold → feedback → completion
-->
