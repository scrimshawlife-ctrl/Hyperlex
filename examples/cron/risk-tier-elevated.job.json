{
  "name": "hyperlex-scan-elevated",
  "description": "Advisory LIVE_EMERGENCE_SCAN for risk tier ELEVATED. Generated by hyperlex.simulation.schedule — operator must register.",
  "skills": ["hyperlex"],
  "skill": "hyperlex",
  "script": "export HERMES_SKILL_DIR=\"${HERMES_SKILL_DIR:-$HOME/.hermes/skills/hyperlex}\"; python3 \"$HERMES_SKILL_DIR/scripts/hyperlex.py\" scan --queries \"sharp money steam revenge,hodl diamond hands rekt degen,brainrot aura farming mid,agentic slop skill issue,based cope seethe,rizz locked in crash out,quiet quitting act your wage,skill issue touch grass ratio\" --source combined --receipt --forecasts --append-log --json; python3 \"$HERMES_SKILL_DIR/scripts/hyperlex.py\" vector-seed --include-home --no-registry 2>/dev/null || true; python3 \"$HERMES_SKILL_DIR/scripts/hyperlex.py\" archive-export --include-home-receipts --history --snapshot-id \"scan-$(date -u +%Y%m%dT%H%M%SZ)\" 2>/dev/null || true",
  "no_agent": true,
  "model": null,
  "provider": null,
  "schedule": {
    "kind": "cron",
    "expr": "0 */2 * * *",
    "display": "0 */2 * * * (every 2h · tier ELEVATED)"
  },
  "schedule_display": "0 */2 * * *",
  "state": "proposed",
  "risk_tier": "ELEVATED",
  "notes": [
    "ADVISORY only — does not auto-register with Hermes cron.",
    "Increase scan frequency; archive + reseed vectors after runs.",
    "Generate fresh plans with: risk-schedule --tier ELEVATED --schedule-out examples/cron/",
    "Scan never invents Brier; settle forecasts via settle / score-series.",
    "Offline: HYPERLEX_OFFLINE=1 and --source mock"
  ]
}
