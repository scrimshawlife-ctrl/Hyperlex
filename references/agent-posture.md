# Agent Posture for Hyperlex

- Smallest working layer: return explicit JSON with provenance.
- Fail closed on weak real ingest (mark as fallback).
- Do not invent signals.
- Receipts are sacred — every serious run produces one.
- Symbolic mappings are proposed, never forced without Orchestra validation.
- Pure real data preferred; mock only for development and testing.