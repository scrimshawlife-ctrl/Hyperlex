[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hyperlex"
version = "1.6.0"
description = "Standalone memetic emergence engine for slang, hyperstition, virality. Real ingest + arXiv-distilled analysis + symbolic architecture (numogram+chaos)."
authors = [
    {name = "Hermes / Abraxas systems"}
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
dependencies = [
    "requests>=2.0",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "jsonschema>=4.0",
]
schema = [
    "jsonschema>=4.0",
]

[project.urls]
Homepage = "https://github.com/scrimshawlife/hyperlex"
Repository = "https://github.com/scrimshawlife/hyperlex"

[project.scripts]
hyperlex = "hyperlex.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
